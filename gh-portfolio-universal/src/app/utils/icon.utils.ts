// generate fontawesome icon string 
export const faIcon = (name: string): string => `fas fa-${name}`;

// display fontawesome brand icons
export const faBrand = (name: string): string => `fa-brands fa-${name}`;
